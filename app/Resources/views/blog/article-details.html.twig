{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}

    <h1 class="text-center mb-5">Détail d'un article</h1>

    <div class="row">
        <article class="post clearfix mb-0 col-md-12  text-center">

            <div class="entry-header">
                <h3 class="entry-title text-uppercase pt-0 mt-0">
                    {{ articleInformation.title }}
                </h3>
            </div>
            <div class="entry-content">
                <p>{{ articleInformation.description }}</p>
            </div>

        </article>
    </div>
    <a class="btn btn-primary btn-sm text-uppercase fa-pull-right" href="{{ path("doctissimo_blog_homepage") }}" >Retour</a>

{% endblock %}